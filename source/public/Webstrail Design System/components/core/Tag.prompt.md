A tag for stack, integrations, and filter chips — clean sans, the engineering-precision flavor.

```jsx
<Tag>MERN</Tag>
<Tag>Open Dental</Tag>
<Tag as="button" active onClick={...}>All work</Tag>
```

Set `active` for selected filter chips; use `as="button"` (or pass `onClick`) to make it interactive.
